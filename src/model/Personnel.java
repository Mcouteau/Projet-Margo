package model;

public class Personnel extends Personne {

	// Constructeur
		public Personnel(String nomPersonne, String prenomPersonne, String situationFam, String adress) {
			super( nomPersonne, prenomPersonne, situationFam, adress);
		}
}
